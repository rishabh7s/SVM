# Direct dependencies only. Transitives are left to pip's resolver -- a full
# `pip freeze` dump pins ~80 packages the codebase never imports, and any one
# of those pins can fail to build on a reviewer's platform.
#
# Versions are the ones this submission was developed and evaluated against.

# Backend
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Interface
streamlit==1.63.0

# Model access: google-genai is the Gemini SDK; instructor wraps it to return
# validated Pydantic models instead of free-form text.
google-genai==2.22.0
instructor==1.16.0
pydantic==2.13.5

# Configuration and HTTP
python-dotenv==1.2.3
requests==2.34.2

# Tests. httpx is required by fastapi.testclient.TestClient, which the API
# contract tests use; it is a test-only dependency but not optional for them.
pytest==9.1.1
httpx==0.28.1
